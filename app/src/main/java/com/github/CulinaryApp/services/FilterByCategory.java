package com.github.CulinaryApp.services;
import retrofit2.http.GET;
import retrofit2.Call;

public interface FilterByCategory {
/* Endpoint: filter.php?c=
 * ... where the name of the category is missing at the end here since it's dynamic...
 */

    /*
    @GET("filter.php?c=")
    Call<>
     */
}
